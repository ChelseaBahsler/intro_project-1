<div class="content container py-5">
  <div class="columns">
    <div class="column">
      <h1 class="title">Supertypes</h1>
      <div class="mt-5">
        <ul style="list-style-type: none;">
          <% for type in @types %>
            <p><%= link_to type.name, "/types/#{type.id}" %></p>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
